# @privy-io/react-auth declares permissionless as a peerOptional, which wants an
# older `ox` than viem 2.55 ships. We don't use smart accounts, so that peer is
# irrelevant -- but npm still refuses the install without this. Committed so
# Vercel resolves dependencies exactly the way local does.
legacy-peer-deps=true
